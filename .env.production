
#AUTH_SECRET="paste-your-generated-secret-here"
#NEXTAUTH_URL="http://localhost:3000"
#NODE_ENV="development"

# --- Database (Dockerized Postgres) ---
# Active: points to the Postgres container in docker-compose.yml
# Host "db" = the service name inside Docker's internal network
#DATABASE_URL="postgresql://crmuser:crmpass@db:xxxx/sangam_crm?schema=public"

# Previous: local/non-Docker Postgres (kept for reference, not in use)
# DATABASE_URL="postgresql://postgres:xxxxxxxxx@localhost:5432/sangam_crm?schema=public"

# --- Auth (NextAuth) ---
# Secret used to sign/encrypt session tokens — generate with: openssl rand -base64 32
# DATABASE_URL="postgresql://postgres:sangam7548@localhost:5432/sangam_crm?schema=public"
DATABASE_URL="postgresql://neondb_owner:npg_7xxxxxxxxxD@ep-still-frxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxre&channel_binding=require"
AUTH_SECRET="xG22xSJxxxxxxxxxxxxxxxxxxxxx0E="

# Base URL NextAuth uses for callbacks — update if deploying elsewhere
NEXTAUTH_URL="https://sangamcrm.netlify.app"

# --- Environment ---
# development | production | test
NODE_ENV="production"

WEBSITE_API_KEY="835d4f9xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx2bb4d18ee8fe"

//Mail connecting to send profiles to clients
SMTP_HOST="smtp.ethereal.email"
SMTP_PORT="587"
SMTP_USER="fxxxxxxxxxxxxxxxjj@ethereal.email"
SMTP_PASS="NxxxxxxxxxxSPdUcsH"
